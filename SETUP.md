# My Trello — セットアップ手順

自分専用の Trello 代替を **完全無料** で動かすための手順です。
データはあなたの Google ドライブ内のスプレッドシートに保存されます。
PC・スマホのどちらからでも、1つの URL を開くだけで使えます。

---

## 完成イメージ

```
[スマホ / PCのブラウザ]
       |  URLを開く
       v
[Apps Script Web App]  ← Google が無料でホスト
       |
       v
[スプレッドシート(=ドライブ内のDB)]
```

---

## セットアップ（所要15分・初回のみ）

### STEP 1. Apps Script プロジェクトを作る
1. ブラウザで https://script.google.com を開く（自分の Google アカウントでログイン）
2. 左上の **「新しいプロジェクト」** をクリック
3. プロジェクト名を `My Trello` などに変更（左上の「無題のプロジェクト」をクリック）

### STEP 2. ファイルを4つ用意する
左の **ファイル一覧（＋ボタン）** で、このフォルダ内のファイルと同じものを作ります。

| Apps Script上で作るファイル | 種類 | このフォルダの中身をコピペ |
|---|---|---|
| `Code.gs`（最初からある）  | スクリプト | `apps-script/Code.gs` |
| `Index`        | HTML | `apps-script/Index.html` |
| `Stylesheet`   | HTML | `apps-script/Stylesheet.html` |
| `JavaScript`   | HTML | `apps-script/JavaScript.html` |

- HTMLファイルは「＋」→「HTML」で作成します（**ファイル名に .html は付けません**。`Index` のように入力すると自動で `Index.html` になります）。
- 各ファイルを開き、中身を**全選択して削除 → このフォルダの対応ファイルの中身を貼り付け**ます。

> （任意）`appsscript.json` を表示するには、左の歯車「プロジェクトの設定」→「`appsscript.json` をエディタで表示」にチェック。中身を `apps-script/appsscript.json` で置き換えると、タイムゾーン等が整います。

### STEP 3. デプロイ（公開）する
1. 右上の **「デプロイ」 → 「新しいデプロイ」**
2. 歯車アイコン → **「ウェブアプリ」** を選択
3. 設定:
   - 説明: `v1`
   - 次のユーザーとして実行: **自分**
   - アクセスできるユーザー: **自分のみ**
4. **「デプロイ」** をクリック
5. 初回は権限の確認が出ます → 自分のアカウントを選択 →
   「このアプリは Google で確認されていません」と出たら **「詳細」→「（プロジェクト名）に移動」→「許可」**
   （自分が作ったアプリなので安全です）
6. 表示された **ウェブアプリの URL** をコピー → これがあなたのボードのアドレスです 🎉

### STEP 4. スマホ・PCから使えるようにする
- **PC**: その URL をブックマーク
- **スマホ（iPhone/Android）**: その URL を Chrome / Safari で開き、
  「ホーム画面に追加」すると **アプリのように** 起動できます

初回アクセス時に、データ保存用のスプレッドシート `My Trello DB` が
自動でドライブに作成されます（中身を直接いじる必要はありません）。

---

## 使い方
- **リスト追加**: 上部の「＋ リスト追加」
- **カード追加**: 各リスト下部の「＋ カードを追加」
- **並べ替え**: カードをドラッグ（リスト間移動も可）／リストはタイトル部分をドラッグ
- **カード編集**: カードをクリック → ラベル・期限・説明・チェックリスト・コメントを編集
- すべて自動保存されます

---

## コードを更新したとき（重要）
ファイルを書き換えたら、反映には **再デプロイ** が必要です。
「デプロイ → デプロイを管理 → （鉛筆アイコンで編集）→ バージョン:新しいバージョン → デプロイ」。
URL は変わりません。

---

## よくある質問
- **Q. データはどこ?** → ドライブ内の `My Trello DB` というスプレッドシート。Google が自動バックアップ。
- **Q. 料金は?** → 完全無料（個人利用の範囲で十分すぎる無料枠内）。
- **Q. 他人に見られない?** → アクセスを「自分のみ」にしているので、あなたのログイン以外からは開けません。

---

## 実装済みの機能（追加分）
- 複数ボード（上部メニューで切り替え／追加・名前変更・削除）
- 各カードへの添付ファイル（ドライブの `My Trello Attachments` フォルダに保存）
  - ※ 添付機能のためドライブへのアクセス権が必要。**コード更新後の再デプロイ時に権限確認が出たら「許可」**してください。
- Trello 参考のデザイン（文字入りラベル、2カラムのカード詳細画面）

## 次のステップ（v2 で追加予定）
- カレンダー表示（期限のあるカードを月表示）
- 自動化ルール（例: 「完了」リストに移したらチェックを全部ON 等）

実装したくなったら、この `SETUP.md` と `apps-script/` 一式を見ながら相談してください。
